# Runtime dependencies for Streamlit Community Cloud.
# Pinned to the versions in the project .venv (Python 3.12).
# Dev/transcription-only tooling (pytest, pymupdf, pdfplumber) is intentionally excluded.
streamlit==1.58.0
altair==6.2.2
pandas==3.0.3
# Pure-Python component (py3-none-any wheel, Requires-Python >=3.7) backing
# app/_local_store.py: browser-localStorage profile persistence so saved
# dive profiles are private per visitor instead of shared server-side disk.
streamlit-local-storage==0.0.25
